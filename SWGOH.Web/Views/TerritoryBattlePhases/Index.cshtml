@model IEnumerable<SWGOH.Entities.TerritoryBattlePhase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Territory1.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Territory2.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Territory3.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequiredStars)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HasSecondTerritory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Territory1.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Territory2.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Territory3.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RequiredStars)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HasSecondTerritory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
